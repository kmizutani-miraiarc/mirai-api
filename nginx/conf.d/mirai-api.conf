# Mirai API サーバー設定
# /etc/nginx/conf.d/mirai-api.conf に配置

# レート制限
limit_req_zone $binary_remote_addr zone=mirai_api:10m rate=10r/s;

# アップストリーム設定（直接Python実行）
upstream mirai_api {
    server 127.0.0.1:8000;
}

# HTTPからHTTPSへのリダイレクト
server {
    listen 80;
    server_name your-domain.com www.your-domain.com;
    
    # ファイルアップロード用のサイズ制限を設定（HTTPでも設定）
    client_max_body_size 200M;
    
    # Let's Encrypt認証用（重要: 最初に配置）
    location /.well-known/acme-challenge/ {
        root /var/www/mirai-api;
        try_files $uri =404;
    }
    
    # APIパスのみHTTPSにリダイレクト
    location /api/ {
        client_max_body_size 200M;
        return 301 https://$host$request_uri;
    }
    
    # その他のパスは既存サービスに委譲
    location / {
        client_max_body_size 200M;
        # 既存のプロキシ設定またはrootディレクトリ
        # proxy_pass http://existing-service;
        # または
        root /var/www/mirai-api;
        try_files $uri $uri/ =404;
    }
}

# HTTPS設定
server {
    listen 443 ssl http2;
    server_name your-domain.com www.your-domain.com;

    # SSL証明書設定（既存証明書を使用する場合）
    # 既存のLet's Encrypt証明書を使用
    ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
    
    # または、既存の証明書パスを指定
    # ssl_certificate /path/to/existing/cert.pem;
    # ssl_certificate_key /path/to/existing/key.pem;
    
    # SSL設定
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES128-SHA256:ECDHE-RSA-AES256-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    # セキュリティヘッダー
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options DENY;
    add_header X-Content-Type-Options nosniff;
    add_header X-XSS-Protection "1; mode=block";
    add_header Referrer-Policy "strict-origin-when-cross-origin";

    # ファイルアップロード用のサイズ制限を設定
    client_max_body_size 200M;
    
    # API プロキシ設定（パスベースの場合）
    location /api/ {
        limit_req zone=mirai_api burst=20 nodelay;
        
        # ファイルアップロード用に大きなサイズを許可
        client_max_body_size 200M;
        
        proxy_pass http://mirai_api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # タイムアウト設定（大きなファイルアップロード用に延長）
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
        
        # バッファ設定（大きなファイル用に調整）
        proxy_buffering off;  # 大きなファイルの場合はバッファリングをOFFにする
        proxy_request_buffering off;  # リクエストボディのバッファリングをOFF
    }
    
    # ルートパス（既存サービスに委譲）
    location / {
        # ファイルアップロード用に大きなサイズを許可
        client_max_body_size 200M;
        
        # 既存のプロキシ設定またはrootディレクトリ
        # proxy_pass http://existing-service;
        # または
        # root /var/www/html;
    }

    # ヘルスチェック
    location /health {
        proxy_pass http://mirai_api/health;
        access_log off;
    }

    # 静的ファイル（必要に応じて）
    location /static/ {
        alias /var/www/mirai-api/static/;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
